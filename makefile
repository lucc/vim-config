# makefile for ~/.config/vim by luc

include generic.mk

CONFIGS = \
	  .$(SEP).vim                 \
	  gvimrc$(SEP).gvimrc         \
	  vimpagerrc$(SEP).vimpagerrc \
	  vimrc$(SEP).vimrc           \
